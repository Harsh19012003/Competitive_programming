#include <bits/stdc++.h>
#include <vector>
#include <map>

using namespace std;

int main(){

    // for fast input output
    ios::sync_with_stdio(0);
    cin.tie(0);
    

    int n;
    cin >> n;

    for (int i = 0; i < n; i++){
        string str;
        cin >> str;

        for(char i: str){
            cout << i;
        }

        stack<int> stk;

        

    }

    return 0;
}