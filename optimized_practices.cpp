// include each std lib
#include <bits/stdc++.h>

using namespace std;

int main(){
    // fast input output
    ios::sync_with_stdio(0);
    cin.tie(0);

    // use "\n" then endl

    // macro define to reduce words
    int n = 10;
    #define loop(a,b) for(int i = a; i < b; i++)

    loop(0,5) {
        cout << i <<"\n";
    }
}